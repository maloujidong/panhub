# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Runtime data (SQLite database, hot-searches)
data/

# Tooling / IDE local data (never project source)
.claude/
.codebuddy/
.playwright-mcp/
.workbuddy/

# Vitest config-transform temp files (regenerated on every test run)
vitest.config.ts.timestamp-*.mjs

# WeChat mini-program: "构建 npm" output (regenerated locally)
miniprogram/miniprogram_npm/

# Guard: this project uses npm, NOT pnpm — ignore accidental pnpm artifacts
pnpm-lock.yaml
pnpm-workspace.yaml
